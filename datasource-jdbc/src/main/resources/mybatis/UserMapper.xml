<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.target.datasource.mapper.UserMapper">
    <select id="selectBlog" resultType="map">
        select * from traininfo where METROLINEID = #{id}
    </select>

    <select id="selectBlog2" resultType="com.target.datasource.bean.TrainInfo">
        select * from traininfo where METROLINEID = #{id}
    </select>
</mapper>